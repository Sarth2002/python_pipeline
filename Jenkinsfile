pipeline {
    agent any
    stages {
        stage('version') {
            steps {
                sh 'python'
            }
        }
        stage('hello'){
            steps{
                sh 'python hello.py'
            }
        }
    }
}
